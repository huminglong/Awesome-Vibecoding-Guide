# Vibecoding故障排除指南 🔧

**常见AI辅助开发问题的快速解决方案**

---

## 目录

1. [快速症状查找](#快速症状查找)
2. [紧急流程图](#紧急流程图)
3. [AI行为问题](#ai行为问题)
4. [代码质量问题](#代码质量问题)
5. [工作流程瓶颈](#工作流程瓶颈)
6. [工具特定问题](#工具特定问题)
7. [常见错误模式](#常见错误模式)
8. [调试检查清单](#调试检查清单)
9. [何时寻求帮助](#何时寻求帮助)

---

## 快速症状查找

**找到您的问题 → 获得快速修复 → 查看详细解决方案**

### AI行为问题

| 症状                 | 可能原因             | 快速修复                | 详细信息                                                                         |
| -------------------- | -------------------- | ----------------------- | -------------------------------------------------------------------------------- |
| AI忘记以前的决策     | 上下文溢出>85%       | 外部化到.md文件         | [上下文管理](../context-management/README-zh.md/#the-60-85-rule)                        |
| 生成无法编译的代码   | 缺少上下文或语法错误 | 提供编译器错误 + 上下文 | [错误恢复](#error-recovery-prompting)                                            |
| 建议过时的模式       | 未加载项目上下文     | 创建.cursorrules文件    | [上下文感知提示](../prompting/advanced-techniques-zh.md#context-aware-prompting) |
| 捏造不存在的API/函数 | 幻觉                 | 具体化，引用真实代码    | [具体性问题](#hallucination-apis-that-dont-exist)                                |
| 矛盾的建议           | 上下文混淆           | 使用摘要重新开始会话    | [会话重置](#session-reset-recovery)                                              |
| 问太多澄清问题       | 模糊的提示           | 使用提示模板            | [提示模板](../prompting/template-library-zh.md)                                  |
| 实施错误的功能       | 误解需求             | 添加具体示例            | [示例驱动提示](#ai-misunderstands-requirements)                                  |
| 忽略约束             | 约束不明确           | 使用带强调的编号列表    | [约束沟通](#ai-ignores-constraints)                                              |
| 过度设计解决方案     | 没有简单性指导       | 添加"保持简单" + 约束   | [过度设计](#over-engineering)                                                    |
| 生成不安全的代码     | 未提及安全性         | 使用安全检查清单        | [安全审查](#insecure-code-generation)                                            |
| 响应缓慢             | 上下文太大           | 删除不相关文件          | [上下文优化](#slow-ai-responses)                                                 |
| 不完整的响应（中断） | 达到上下文限制       | 分解为更小的提示        | [响应截断](#incomplete-responses)                                                |
| 与项目不同的风格     | 未引用样式指南       | 添加样式示例            | [代码风格与项目不匹配](#code-style-doesnt-match-project)                         |
| 无法调试复杂问题     | 数据不足             | 提供日志 + 堆栈跟踪     | [调试指南](#complex-debugging-failures)                                          |
| 建议错误的库/工具    | 缺乏项目知识         | 前期指定技术栈          | [工具选择](#wrong-library-suggestions)                                           |

---

### 代码质量问题

| 症状                     | 可能原因       | 快速修复         | 详细信息                                                          |
| ------------------------ | -------------- | ---------------- | ----------------------------------------------------------------- |
| 第一次尝试就有错误的代码 | 模糊的需求     | 使用详细提示     | [代码质量部分](#buggy-code-on-first-generation)                   |
| 缺少错误处理             | 提示中未指定   | 添加错误处理要求 | [错误处理](#missing-error-handling)                               |
| 没有输入验证             | 未提及安全性   | 指定验证规则     | [输入验证](#missing-input-validation)                             |
| 糟糕的变量命名           | 没有样式指南   | 提供命名示例     | [命名约定](#poor-variable-naming)                                 |
| 重复的代码               | 没有DRY指令    | 请求抽象         | [代码重复](#code-duplication)                                     |
| 硬编码的值               | 没有配置指导   | 请求配置提取     | [硬编码的值](#hard-coded-values)                                  |
| 缺少边缘情况处理         | 未提及情况     | 明确列出边缘情况 | [边缘情况](#missing-edge-cases)                                   |
| 性能问题                 | 未优先考虑效率 | 添加性能要求     | [性能问题](#poor-performance)                                     |
| 没有生成测试             | 未请求测试     | 使用测试模板     | [测试模板](../prompting/template-library-zh.md#testing-templates) |
| SQL注入漏洞              | 未审查安全性   | 添加安全审计     | [SQL注入](#sql-injection-vulnerabilities)                         |
| XSS漏洞                  | 输入未清理     | 指定清理         | [XSS预防](#xss-vulnerabilities)                                   |
| 内存泄漏                 | 未指定清理     | 添加清理要求     | [内存泄漏](#memory-leaks)                                         |
| 竞争条件                 | 未解决并发     | 指定同步要求     | [竞争条件](#race-conditions)                                      |
| 紧密耦合                 | 未定义架构     | 请求松散耦合     | [紧密耦合](#tight-coupling)                                       |
| 缺少文档                 | 未请求文档     | 使用文档模板     | [文档](#missing-documentation)                                    |

---

### 工作流程瓶颈

| 症状              | 可能原因       | 快速修复                       | 详细信息                                                                       |
| ----------------- | -------------- | ------------------------------ | ------------------------------------------------------------------------------ |
| 太多次迭代        | 糟糕的初始提示 | 学习提示基础                   | [提示指南](../prompting/foundations-zh.md)                                     |
| 上下文经常满      | 不使用.md文件  | 将计划外部化到docs/            | [基于MD的工作流程](../context-management-zh.md/#the-md-based-workflow-pattern) |
| 会话之间丢失工作  | 没有会话交接   | 编写交接文档                   | [会话交接](../context-management-zh.md/#session-handoffs)                      |
| 开发节奏慢        | 顺序而不是并行 | 使用并行提示                   | [并行提示](../prompting/advanced-techniques-zh.md#parallel-prompting)          |
| 重复相同的解释    | 没有持久指令   | 创建.cursorrules               | [项目上下文](#repeating-same-instructions)                                     |
| 无法轻松恢复工作  | 没有文档       | 编写current-state.md           | [可恢复性](#difficulty-resuming-work)                                          |
| 调试时间太长      | 诊断信息不足   | 使用调试模板                   | [调试模板](../prompting/template-library-zh.md#debugging-templates)            |
| 破坏现有功能      | 没有集成测试   | 添加测试要求                   | [集成测试](#breaking-existing-features)                                        |
| 下一步不清楚      | 没有任务分解   | 使用TodoWrite/Task Manager MCP | [任务管理](#unclear-next-steps)                                                |
| 被复杂性压倒      | 大任务未分解   | 使用多步骤工作流程             | [多步骤工作流程](../prompting/advanced-techniques-zh.md#multi-step-workflows)  |
| 代码风格不一致    | 没有记录约定   | 编写样式指南                   | [样式指南创建](#inconsistent-code-style)                                       |
| 忘记项目细节      | 没有知识库     | 创建project-knowledge.md       | [项目记忆](#forgetting-project-details)                                        |
| MCP服务器没有帮助 | 任务的MCP错误  | 检查MCP决策树                  | [MCP使用](../context-management-zh.md/#mcp-decision-tree)                      |
| 高AI成本          | 提示效率低     | 优化提示                       | [成本优化](#high-ai-costs)                                                     |
| 团队不一致        | 没有共享实践   | 创建团队模板                   | [团队协调](#team-inconsistent-results)                                         |

---

### 工具特定问题

| 工具                | 症状           | 快速修复         | 详细信息                                        |
| ------------------- | -------------- | ---------------- | ----------------------------------------------- |
| **Claude Code CLI** | 找不到文件     | 使用Glob工具     | [文件搜索](#claude-code-file-search-issues)     |
| **Claude Code CLI** | 性能缓慢       | 检查上下文使用   | [性能](#claude-code-slow-performance)           |
| **Claude Code CLI** | 任务代理失败   | 检查代理类型     | [代理选择](#claude-code-agent-issues)           |
| **Cursor**          | Composer失败   | 减少文件数量     | [Composer问题](#cursor-composer-failures)       |
| **Cursor**          | 错误的文件更改 | 更具体           | [文件选择](#cursor-wrong-files)                 |
| **Windsurf**        | Cascade混淆    | 清除上下文       | [Cascade重置](#windsurf-cascade-problems)       |
| **Zed**             | 工作流程不工作 | 检查工作流程语法 | [工作流程调试](#zed-workflow-issues)            |
| **Droid CLI**       | 计划模式卡住   | 分解为更小计划   | [计划问题](#droid-plan-mode-problems)           |
| **Droid CLI**       | 执行模式失败   | 审查计划清晰度   | [执行问题](#droid-act-mode-failures)            |
| **MCP DevTools**    | 无法连接       | 检查浏览器状态   | [DevTools连接](#devtools-mcp-connection-issues) |
| **MCP Context7**    | 返回错误的文档 | 更好地指定查询   | [Context7查询](#context7-mcp-issues)            |
| **OpenSpec**        | 规范质量差     | 提供更多细节     | [规范生成](#openspec-quality-issues)            |
| **Git集成**         | 提交失败       | 检查权限         | [Git问题](#git-integration-issues)              |

---

## 紧急流程图

### 流程图1：AI生成垃圾代码

```
AI生成无法使用的代码？
│
├─ 检查1：上下文是否> 85%满？
│  ├─ 是 → 外部化到.md文件
│  │        使用交接开始新会话
│  │        ✓ 问题解决
│  │
│  └─ 否 → 继续检查2
│
├─ 检查2：您的提示是否模糊？
│  ├─ 是 → 使用4组件框架
│  │        - 清晰度：您想要什么
│  │        - 上下文：AI需要什么
│  │        - 约束：边界
│  │        - 标准：成功指标
│  │        ✓ 用更好的提示重试
│  │
│  └─ 否 → 继续检查3
│
├─ 检查3：AI是否有项目上下文？
│  ├─ 否  → 加载架构文档
│  │        引用现有模式
│  │        创建.cursorrules
│  │        ✓ 带上下文重试
│  │
│  └─ 是 → 继续检查4
│
├─ 检查4：这是否是任务的正确模型？
│  ├─ 否  → 切换到更好的模型
│  │        （Claude Sonnet/Opus用于复杂任务）
│  │        （GLM/Qwen用于标准任务）
│  │        ✓ 用更好的模型重试
│  │
│  └─ 是 → 继续检查5
│
└─ 检查5：任务是否太复杂，无法在一个提示中完成？
   ├─ 是 → 分解为多步骤工作流程
```

---

### 流程图2：无法调试问题

```
调试卡住> 30分钟？
│
├─ 您是否提供了错误消息+堆栈跟踪？
│  ├─ 否  → 收集诊断信息：
│  │        - 完整错误消息
│  │        - 堆栈跟踪
│  │        - 相关代码（文件:行）
│  │        - 您在做什么
│  │        ✓ 提供给AI，重试
│  │
│  └─ 是 → 继续
│
├─ 您是否尝试了最小复现？
│  ├─ 否  → 创建最小测试用例
│  │        移除不相关代码
│  │        隔离问题
│  │        ✓ 调试最小用例
│  │
│  └─ 是 → 继续
│
├─ 您是否检查了日志/控制台？
│  ├─ 否  → 检查：
│  │        - 浏览器控制台（F12）
│  │        - 服务器日志
│  │        - 网络标签
│  │        - 数据库日志
│  │        ✓ 收集所有错误信息
│  │
│  └─ 是 → 继续
│
├─ 您是否搜索了类似问题？
│  ├─ 否  → 搜索：
│  │        - Google中的错误消息
│  │        - GitHub问题
│  │        - Stack Overflow
│  │        ✓ 尝试建议的解决方案
│  │
│  └─ 是 → 继续
│
├─ 您是否用完整上下文询问了AI？
│  ├─ 否  → 使用调试模板：
│  │        - 错误+堆栈跟踪
│  │        - 相关代码
│  │        - 您尝试了什么
│  │        - 预期vs实际
│  │        ✓ AI现在可以帮助
│  │
│  └─ 是 → 是时候询问人类了
│           → 参见"何时寻求帮助"部分
```

---

### 流程图3：高令牌使用/成本

```
令牌成本太高？
│
├─ 您是否在每个提示中重复上下文？
│  ├─ 是 → 外部化到.md文件
│  │        引用而不是重复
│  │        使用.cursorrules作为约定
│  │        ✓ 节省70-90%令牌
│  │
│  └─ 否 → 继续
│
├─ 您是否加载了太多文件？
│  ├─ 是 → 渐进式上下文加载
│  │        仅包含需要的内容
│  │        根据需要添加更多文件
│  │        ✓ 节省50-80%令牌
│  │
│  └─ 否 → 继续
│
├─ 提示是否过于冗长？
│  ├─ 是 → 使用模板
│  │        引用文档
│  │        简洁但具体
│  │        ✓ 节省40-60%令牌
│  │
│  └─ 否 → 继续
│
├─ 您是否在单次有效时使用多步骤？
│  ├─ 是 → 合并相关任务
│  │        使用单个综合提示
│  │        ✓ 减少提示数量
│  │
│  └─ 否 → 继续
│
└─ 是否为简单任务使用昂贵的模型？
   ├─ 是 → 使用更便宜的模型：
```

---

### 流程图4：破坏性更改

```
新代码破坏了现有功能？
│
├─ 您有测试吗？
│  ├─ 否  → 尽快编写测试
│  │        在更改之前和之后运行
│  │        ✓ 防止未来的破坏
│  │
│  └─ 是 → 继续
│
├─ 您在合并之前运行测试了吗？
│  ├─ 否  → 始终运行测试！
│  │        git checkout上一个提交
│  │        ✓ 恢复并测试
│  │
│  └─ 是 → 测试可能不完整
│
├─ 您是否指定了"不要破坏现有功能"？
│  ├─ 否  → 始终指定：
│  │        "保持向后兼容性"
│  │        "不要更改现有API"
│  │        ✓ AI会更小心
│  │
│  └─ 是 → 继续
│
├─ 您在提交之前审查了更改吗？
│  ├─ 否  → 始终审查差异
│  │        检查意外更改
│  │        ✓ 尽早发现问题
│  │
│  └─ 是 → 继续
│
└─ 范围是否太宽？
   ├─ 是 → 分解为更小的更改
```

---

## 🧠 人类上下文问题

**大多数调试失败不是AI失败——它们是上下文失败。**

### 95/5规则

如果您被迫"重新开始"一个项目，您要么是：
- **5%** - 在企业级别开发超级复杂的东西
- **95%** - 试图强行前进而不是正确地指导AI

**真正的问题**：人类缺乏上下文知识，而不是AI窗口。

### 通常缺少什么

❌ 没有复制控制台错误
❌ 没有检查网络请求
❌ 没有记录复现步骤
❌ 没有解释预期vs实际行为
❌ 没有提供环境细节

**结果**：AI盲目猜测。您浪费数小时。什么都没用。

### 修复：人类优先调试

**在要求AI调试之前：**

1. **打开DevTools**（F12）
2. **检查控制台** - 复制所有错误
3. **检查网络** - 找到失败的请求
4. **复现** - 编写确切步骤
5. **收集上下文** - 错误+行为+环境

**然后为AI提供完整画面：**
```
"收到此错误：[粘贴确切错误]

控制台：[错误消息+堆栈跟踪]
网络：[失败的请求+响应]
复现：[确切步骤]
预期：[应该发生什么]
实际：[发生了什么]
代码：[文件:行]

你能识别根本原因吗？"
```

### 为什么这很重要

**没有上下文：**
- AI做20次猜测
- 您尝试20次"修复"
- 仍然损坏
- 您责怪AI

**有上下文：**
- AI立即识别根本原因
- 一次有针对性的修复
- 问题解决
- 继续前进

### 重新开始很少是答案

**您不会在传统开发中丢弃一个20小时的项目。**

**在AI辅助开发中也不要这样做。**

- ✅ 新聊天窗口（新上下文）- 好
- ❌ 新项目（丢弃工作）- 坏

**修复流程（上下文收集），而不是项目。**

---

**📖 完整指南**：[AI调试中的人类上下文问题](human-context-debugging-zh.md)

学习：
- 真实世界示例（之前/之后）
- 完整的上下文收集检查清单
- 如何引导AI进行复杂调试
- DevTools MCP等工具何时有帮助
- 常见场景和解决方案

**5分钟阅读 = 调试节省数小时**

---

## AI行为问题

### AI误解需求

**症状**：AI实现了与您想要的完全不同的东西。

**根本原因：**
1. 提示中的语言模糊
2. 缺少示例
3. 没有定义成功标准
4. 技术术语的使用与AI期望的不同

**解决方案：**

**解决方案1：使用示例**
> **不好**："创建一个带有验证的表单。"
>
> **好**："创建一个具有这些确切行为的表单：
> - 电子邮件字段：在模糊时验证格式，在字段下方显示错误'无效电子邮件'。
> - 提交禁用，直到所有字段有效。
> - 示例：用户输入'test@' → 模糊 → 红色边框+显示错误。"

**解决方案2：定义成功标准**
> 添加到每个提示：
>
> "成功意味着：
> - [具体可衡量的结果1]
> - [具体可衡量的结果2]
> - [具体可衡量的结果3]"

**解决方案3：展示，不要只是告诉**
> 不要说："让它看起来专业。"
>
> 展示："使用这个设计：[截图或设计的链接]。"
>
> 或者："匹配[现有组件]的风格。"

**预防：**
- 阅读[提示基础](../prompting/foundations-zh.md#原则2展示不要只是告诉)
- 使用[提示模板](../prompting/template-library-zh.md)
- 始终包括具体示例

---

### 幻觉：不存在的API

**症状**：AI使用项目或库中不存在的函数/API生成代码。

**示例：**
```typescript
// AI生成：
import { magicValidator } from 'my-app/validators'; // 不存在！

user.validateMagically(); // 不存在！
```

**根本原因：**
1. AI基于模式猜测
2. 将您的项目与类似项目混淆
3. 训练数据过时

**解决方案：**

**解决方案1：引用真实代码**
> 使用`src/utils/validation.ts`中的现有验证模式：
>
> ```typescript
> export function validateEmail(email: string): boolean {
>   return EMAIL_REGEX.test(email);
> }
> ```
>
> 不要编造新函数。使用这个确切的模式。

**解决方案2：明确说明存在什么**
> `src/utils/`中的可用实用程序：
> - `validation.ts`：`validateEmail`、`validatePassword`
> - `crypto.ts`：`hash`、`verify`
> - `strings.ts`：`slugify`、`truncate`
>
> **仅**使用这些。不要创建新的实用程序。

**解决方案3：生成后验证**
> 生成代码后，验证所有导入的函数都存在。
>
> 如果不确定，在包含它们之前询问。

**预防：**
- 提供明确的文件引用
- 使用"遵循[现有文件]中的模式"
- 向提示添加验证步骤

---

### AI忽略约束

**症状**：AI生成违反指定约束的代码（文件大小限制、无外部依赖等）

**示例：**
```
您："不要使用任何外部库"
AI：*安装5个npm包*
```

**根本原因：**
1. 约束埋在长提示中
2. 约束陈述微弱（"尽量避免..." vs "绝不能"）
3. AI优先考虑功能而不是约束

**解决方案：**

**解决方案1：强调约束**
> 关键约束（必须遵循）：
> 1. **无**外部依赖（仅使用Node.js内置）。
> 2. 文件大小**最大100行**。
> 3. **无**数据库查询（使用内存数据）。
>
> 如果任何约束不可能，在生成代码之前询问。

**解决方案2：前置约束**
> 构建您的提示：
> 1. 约束**首先**。
> 2. 要求其次。
> 3. 实现细节最后。
>
> 这确保AI在解决方案之前处理约束。

**解决方案3：使用自验证提示**
> 生成代码后，验证：
> - [ ] 未使用外部依赖。
> - [ ] 文件在100行以下。
> - [ ] 无数据库查询。
>
> 如果任何检查失败，在响应之前修改代码。

**预防：**
- 始终首先列出约束
- 使用强语言（"必须"、"要求"、"绝不"）
- 添加验证步骤

---

### 过度设计

**症状**：AI生成过于复杂的解决方案，具有不必要的抽象、设计模式或功能。

**示例：**
```
您："创建一个函数来添加两个数字"
AI：*创建抽象工厂模式，具有接口、依赖注入、策略模式...*
```

**根本原因：**
1. 没有简单性指导
2. AI在企业代码库上训练
3. 没有范围边界

**解决方案：**

**解决方案1：YAGNI原则**
> "创建一个简单的函数来添加两个数字。
>
> 要求：
> - 只是添加两个数字，没有更多。
> - 没有抽象、接口或设计模式。
> - 保持简单（KISS原则）。
> - YAGNI：你不会需要它。
> - 最多5行代码。"

**解决方案2：明确指定范围**
> "**仅**实现当前用例所需的内容。
> 不要添加：
> - 抽象基类。
> - 依赖注入。
> - 策略模式。
> - 配置系统。
> - 日志框架。
>
> 只是解决眼前的问题。"

**解决方案3：引用简单示例**
> "遵循这个示例的简单性：
> [粘贴简单、直接的代码]
>
> 保持相同的简单性水平——没有额外的复杂性。"

**预防：**
- 始终添加"保持简单"
- 指定最大代码行数
- 引用简单示例
- 明确使用YAGNI/KISS原则

---

### 上下文过载：AI忘记事情

**症状**：AI忘记以前的决策，与之前的响应矛盾，或"不记得"您讨论的内容。

**根本原因：**
1. 上下文窗口> 85%满
2. 对话中消息太多
3. 重要信息在对话早期（近期偏差）

**解决方案：**

**解决方案1：会话交接**
> 将您的工作总结写入`docs/context/session-handoff.md`：
> - 实现了什么。
> - 做出的决定（带有推理）。
> - 当前状态。
> - 下一步。
>
> 要全面——您将在之后开始新会话。
>
> **新会话提示**："阅读`docs/context/session-handoff.md`并从我们离开的地方继续。"

**解决方案2：外部化决策**
> 当AI做出重要决定时，将其记录在`docs/decisions/[number]-[topic].md`中：
> - 您决定了什么。
> - 为什么您决定它。
> - 考虑的替代方案。
> - 影响。
>
> 未来的提示："根据`docs/decisions/003-auth-approach.md`，实现……"

**解决方案3：上下文刷新**
> 如果上下文使用超过70%：
>
> "总结我们的关键决定和当前进度。然后我们将用该总结重新开始。"

**预防：**
- 监控上下文使用（60-85%规则）
- 早期外部化到.md文件
- 使用[上下文管理策略](../context-management/README-zh.md)

---

### AI响应缓慢

**症状**：AI需要> 30秒响应，或响应延迟。

**根本原因：**
1. 上下文太大（> 100k令牌）
2. 包含太多文件
3. 需要大量处理的复杂请求
4. API/模型提供商问题

**解决方案：**

**解决方案1：减少上下文**
> "从上下文中删除所有文件，除了：
> - `src/api/users.ts`（与当前任务相关）。
> - `docs/architecture/api-design.md`。
>
> 现在实现[任务]。"

**解决方案2：渐进式加载**
> 不要一次加载所有文件。
> - 从最小上下文加上架构概述开始。
> - 仅在当前步骤需要时添加文件。
> - 任务完成时删除文件。

**解决方案3：使用更快的模型**
> 对于简单任务：
> - 使用GLM（快速、便宜）。
> - 使用Gemini Flash。
> - 使用Qwen Coder。
>
> 对于复杂任务：
> - 使用Claude Sonnet。
> - 使用Claude Opus（仅在必要时）。

**预防：**
- 尽可能保持上下文在50k令牌以下
- 使用[渐进式加载](../prompting/foundations-zh.md#策略1即时上下文加载)
- 为任务复杂性选择正确的模型

---

### 响应不完整（中断）

**症状**：AI响应在句子中间或代码块中间中断。

**根本原因：**
1. 达到响应长度限制
2. 上下文窗口完全满
3. 达到输出令牌限制

**解决方案：**

**解决方案1：要求继续**
> "从您离开的地方继续。"
>
> 或更具体："从第45行继续函数实现。"

**解决方案2：请求更小的块**
> 不要说："实现整个身份验证系统。"
>
> 做："仅实现登录函数（5个步骤中的步骤1）。"
>
> 在移动到下一步之前完成每个步骤。

**解决方案3：减少提示大小**
> 缩短您的提示：
> - 使用模板引用而不是完整要求。
> - 引用文档而不是粘贴内容。
> - 仅关注当前子任务。

**预防：**
- 将大任务分解为步骤
- 保持提示简洁
- 监控上下文使用

---

### 代码风格与项目不匹配

**症状**：AI以与项目不同的风格（制表符vs空格、命名约定、模式）生成代码。

**根本原因：**
1. 没有提供样式指南
2. 没有引用示例代码
3. AI使用通用样式

**解决方案：**

**解决方案1：创建.cursorrules**

> 在项目根目录中创建`.cursorrules`：
>
> ```markdown
> # 代码风格指南
>
> ## 格式化
> - 缩进：2个空格（不是制表符）
> - 引号：字符串使用单引号
> - 分号：必需
> - 行长度：最大100个字符
>
> ## 命名约定
> - 文件：kebab-case（user-service.ts）
> - 组件：PascalCase（UserCard）
> - 函数：camelCase（getUserById）
> - 常量：UPPER_SNAKE_CASE（API_URL）
>
> ## 模式
> - React：仅函数组件（无类）
> - 异步：使用async/await（无.then()）
> - 错误：自定义错误类（不是通用Error）
> - 导入：按类型分组（外部、内部、相对）
>
> ## 示例
> [显示样式的代码片段]
> ```
>
> 现在AI自动遵循这些约定。

**解决方案2：引用样式示例**
> "遵循`src/components/UserCard.tsx`中的确切代码风格：
> - 相同的格式化。
> - 相同的命名模式。
> - 相同的导入结构。
> - 相同的注释风格。"

**解决方案3：请求样式匹配**
> "生成与[现有文件]风格匹配的代码。具体匹配：
> - 缩进。
> - 命名约定。
> - 函数结构。
> - 注释风格。"

**预防：**
- 始终早期创建.cursorrules
- 引用现有文件的模式
- 在提示模板中包含样式要求

---

## 代码质量问题

### 第一次生成就有错误的代码

**症状**：AI生成的代码有明显的错误，无法编译，或运行时立即失败。

**根本原因：**
1. 需求模糊
2. 缺少边缘情况
3. 上下文不完整
4. 模型限制

**解决方案：**

**解决方案1：向提示添加测试用例**
> "创建一个计算阶乘的函数。
>
> 必须处理：
> - `factorial(0) = 1`
> - `factorial(1) = 1`
> - `factorial(5) = 120`
> - `factorial(-1)`抛出`Error('不允许负数')`
> - `factorial(100)`使用`BigInt`处理大数。
>
> 包括演示所有场景正确工作的测试用例。"

**解决方案2：请求自验证**
> "生成代码后，验证：
> 1. 代码编译无错误。
> 2. 所有测试用例通过。
> 3. 处理边缘情况。
> 4. 没有明显的错误。
>
> 如果任何检查失败，在响应之前修改代码。"

**解决方案3：提供完整上下文**
> "这是完整的上下文：
> - 项目使用TypeScript + React
> - 状态管理：Zustand
> - API调用：使用`src/api/client.ts`中的`apiClient`
> - 错误处理：使用`src/utils/errors.ts`中的`AppError`
> - 验证：使用`src/utils/validation.ts`中的`validateEmail`
>
> 基于此上下文实现[功能]。"

**预防：**
- 始终在提示中包含测试用例
- 使用[测试模板](../prompting/template-library-zh.md#测试模板)
- 提供完整的项目上下文

---

### 缺少错误处理

**症状**：AI生成的代码没有try-catch块、错误边界或错误处理逻辑。

**根本原因：**
1. 提示中未指定错误处理
2. AI专注于快乐路径
3. 没有错误处理示例

**解决方案：**

**解决方案1：明确要求错误处理**
> "实现用户注册功能。
>
> **必须包括错误处理：**
> - try-catch块包裹所有异步操作
> - 捕获特定错误类型（网络、验证、数据库）
> - 向用户显示友好的错误消息
> - 记录错误以进行调试
> - 优雅地失败（不崩溃）
>
> 示例错误场景：
> - 网络失败：'无法连接到服务器。请检查您的连接。'
> - 邮件已存在：'此电子邮件已被使用。'
> - 验证失败：'请检查您的输入。'"

**解决方案2：使用错误处理模式**
> "遵循`src/api/users.ts`中的错误处理模式：
>
> ```typescript
> try {
>   const result = await apiClient.post('/users', data);
>   return result;
> } catch (error) {
>   if (error instanceof NetworkError) {
>     throw new AppError('Network error', error);
>   }
>   if (error instanceof ValidationError) {
>     throw new AppError('Validation failed', error);
>   }
>   throw new AppError('Unknown error', error);
> }
> ```
>
> 使用此确切模式。"

**预防：**
- 始终在提示中要求错误处理
- 引用现有的错误处理模式
- 使用[错误处理模板](../prompting/template-library-zh.md)

---

### 缺少输入验证

**症状**：AI生成的代码接受用户输入而不验证，导致安全漏洞或运行时错误。

**根本原因：**
1. 安全性未提及
2. 没有指定验证规则
3. AI专注于功能而非安全

**解决方案：**

**解决方案1：指定验证规则**
> "创建登录表单。
>
> **验证要求：**
> - 电子邮件：必须是有效格式，最大100个字符
> - 密码：最少8个字符，必须包含1个数字和1个特殊字符
> - 在提交之前验证所有字段
> - 在字段下方显示验证错误
> - 禁用提交按钮，直到所有字段有效
>
> **安全要求：**
> - 清理所有输入（防止XSS）
> - 对密码使用bcrypt哈希
> - 限制登录尝试（速率限制）"

**解决方案2：使用验证库**
> "使用`src/utils/validation.ts`中的验证函数：
> - `validateEmail(email: string): boolean`
> - `validatePassword(password: string): ValidationResult`
> - `sanitizeInput(input: string): string`
>
> 在处理任何用户输入之前使用这些函数。"

**预防：**
- 始终指定验证规则
- 提及安全要求
- 使用现有的验证实用程序

---

### 糟糕的变量命名

**症状**：AI生成的代码使用模糊、不描述性的变量名（`x`、`temp`、`data1`）。

**根本原因：**
1. 没有样式指南
2. 没有命名约定
3. AI使用通用名称

**解决方案：**

**解决方案1：指定命名约定**
> "实现用户搜索功能。
>
> **命名约定：**
> - 变量：camelCase（`searchQuery`、`filteredUsers`）
> - 常量：UPPER_SNAKE_CASE（`MAX_RESULTS`、`API_ENDPOINT`）
> - 函数：camelCase（`searchUsers`、`filterByRole`）
> - 布尔值：以`is`/`has`/`should`开头（`isLoading`、`hasResults`）
> - 避免缩写（使用`user`而不是`usr`）
> - 避免单字母变量（使用`index`而不是`i`）"

**解决方案2：引用命名示例**
> "遵循`src/components/UserList.tsx`中的命名模式：
> - `users`（用户数组）
> - `isLoading`（加载状态）
> - `error`（错误消息）
> - `handleSearch`（事件处理程序）
> - `filteredUsers`（过滤结果）
>
> 使用相同的命名约定。"

**预防：**
- 在.cursorrules中指定命名约定
- 引用现有代码的命名模式
- 避免模糊的变量名

---

### 代码重复

**症状**：AI在多个地方重复相同的代码，而不是创建可重用函数。

**根本原因：**
1. 没有DRY指令
2. 没有要求抽象
3. AI专注于完成任务，而不是优化

**解决方案：**

**解决方案1：要求DRY原则**
> "实现用户管理功能。
>
> **DRY要求：**
> - 不要重复代码
> - 为重复逻辑创建可重用函数
> - 提取常量到配置文件
> - 使用组合而不是复制粘贴
>
> 如果您发现自己在复制代码，创建一个函数或组件。"

**解决方案2：要求抽象**
> "为用户创建CRUD操作。
>
> **抽象要求：**
> - 创建一个通用的`apiCall`函数来处理所有API请求
> - 创建一个通用的`handleError`函数来处理错误
> - 创建一个通用的`validateField`函数来验证输入
> - 使用这些通用函数而不是重复代码"

**预防：**
- 始终要求DRY原则
- 要求抽象和可重用性
- 审查生成的代码是否有重复

---

### 硬编码的值

**症状**：AI生成的代码包含硬编码的值（URL、密钥、配置）。

**根本原因：**
1. 没有配置指导
2. AI专注于快速实现
3. 没有环境变量示例

**解决方案：**

**解决方案1：要求配置提取**
> "实现API客户端。
>
> **配置要求：**
> - 所有URL、密钥和设置必须在配置文件中
> - 使用环境变量（`process.env`或`import.meta.env`）
> - 创建`src/config/api.ts`来存储配置
> - 不要硬编码任何值
>
> 示例配置：
> ```typescript
> export const API_CONFIG = {
>   BASE_URL: import.meta.env.VITE_API_URL,
>   TIMEOUT: 5000,
>   RETRY_ATTEMPTS: 3,
> };
> ```"

**解决方案2：使用环境变量**
> "使用环境变量进行配置：
>
> - `VITE_API_URL`：API基础URL
> - `VITE_API_KEY`：API密钥
> - `VITE_MAX_RESULTS`：最大结果数
>
> 在`.env`文件中定义这些变量，并在代码中使用`import.meta.env`访问它们。"

**预防：**
- 始终要求配置提取
- 使用环境变量
- 创建配置文件

---

### 缺少边缘情况处理

**症状**：AI生成的代码处理正常情况，但在边缘情况下失败（空数组、null值、极端输入）。

**根本原因：**
1. 边缘情况未提及
2. AI专注于快乐路径
3. 没有边缘情况列表

**解决方案：**

**解决方案1：列出边缘情况**
> "实现数组搜索功能。
>
> **必须处理这些边缘情况：**
> - 空数组：返回空结果
> - null/undefined输入：抛出错误或返回默认值
> - 空搜索查询：返回所有结果
> - 特殊字符：正确处理
> - 大小写敏感：可选（指定）
> - 超长查询：截断或限制
>
> 为每个边缘情况提供测试用例。"

**解决方案2：要求防御性编程**
> "实现用户数据获取。
>
> **防御性编程要求：**
> - 检查null/undefined值
> - 提供默认值
> - 验证数据结构
> - 处理空数组
> - 记录意外情况
>
> 假设任何东西都可能失败或为null。"

**预防：**
- 始终列出边缘情况
- 要求防御性编程
- 为边缘情况提供测试用例

---

### 性能问题

**症状**：AI生成的代码效率低下，导致缓慢的渲染、内存泄漏或高CPU使用。

**根本原因：**
1. 效率未优先考虑
2. 没有性能要求
3. AI专注于功能而非优化

**解决方案：**

**解决方案1：指定性能要求**
> "实现大型列表渲染。
>
> **性能要求：**
> - 使用虚拟化（react-window或react-virtualized）
> - 实现延迟加载（分页或无限滚动）
> - 避免不必要的重新渲染（使用React.memo、useMemo、useCallback）
> - 优化图像（懒加载、WebP格式）
> - 限制同时渲染的项目（最多50个可见）
> - 目标：60fps滚动性能"

**解决方案2：要求优化**
> "实现数据获取功能。
>
> **优化要求：**
> - 使用缓存（避免重复请求）
> - 实现请求去抖动
> - 使用Web Workers进行繁重计算
> - 优化数据库查询（索引、限制）
> - 使用代码分割（动态导入）
> - 最小化包大小"

**预防：**
- 始终指定性能要求
- 要求优化技术
- 使用性能分析工具

---

### 没有生成测试

**症状**：AI生成的代码没有测试，或测试不完整。

**根本原因：**
1. 测试未请求
2. AI专注于实现而非测试
3. 没有测试要求

**解决方案：**

**解决方案1：要求测试**
> "实现用户注册功能。
>
> **测试要求：**
> - 为所有函数编写单元测试
> - 为组件编写集成测试
> - 为关键流程编写E2E测试
> - 测试正常情况和边缘情况
> - 使用Jest和React Testing Library
> - 目标：80%+代码覆盖率"

**解决方案2：使用测试模板**
> "使用此测试模板：
>
> ```typescript
> describe('UserRegistration', () => {
>   it('should register user with valid data', async () => {
>     // 测试代码
>   });
>
>   it('should reject invalid email', async () => {
>     // 测试代码
>   });
>
>   it('should handle network errors', async () => {
>     // 测试代码
>   });
> });
> ```
>
> 为所有功能创建类似的测试。"

**预防：**
- 始终要求测试
- 使用[测试模板](../prompting/template-library-zh.md#测试模板)
- 指定测试覆盖率目标

---

### SQL注入漏洞

**症状**：AI生成的代码容易受到SQL注入攻击。

**根本原因：**
1. 安全性未审查
2. 使用字符串拼接
3. 没有参数化查询

**解决方案：**

**解决方案1：要求参数化查询**
> "实现用户搜索功能。
>
> **安全要求：**
> - 使用参数化查询（不是字符串拼接）
> - 使用准备语句
> - 验证和清理所有输入
> - 使用ORM或查询构建器（Prisma、Drizzle）
> - 不要直接执行原始SQL
>
> 示例：
> ```typescript
> // ❌ 不好
> const query = `SELECT * FROM users WHERE name = '${name}'`;
>
> // ✅ 好
> const query = 'SELECT * FROM users WHERE name = ?';
> db.query(query, [name]);
> ```"

**解决方案2：使用ORM**
> "使用Prisma ORM进行数据库操作：
>
> ```typescript
> const users = await prisma.user.findMany({
>   where: {
>     name: searchQuery,
>   },
> });
> ```
>
> Prisma自动处理SQL注入防护。"

**预防：**
- 始终要求参数化查询
- 使用ORM或查询构建器
- 审查生成的代码的安全性

---

### XSS漏洞

**症状**：AI生成的代码容易受到跨站脚本攻击。

**根本原因：**
1. 输入未清理
2. 不安全地渲染用户内容
3. 没有转义

**解决方案：**

**解决方案1：要求输入清理**
> "实现评论功能。
>
> **安全要求：**
> - 清理所有用户输入（使用DOMPurify）
> - 转义HTML特殊字符
> - 使用React的自动转义（不要使用dangerouslySetInnerHTML）
> - 验证和限制输入长度
> - 实施内容安全策略（CSP）
>
> 示例：
> ```typescript
> import DOMPurify from 'dompurify';
>
> const cleanComment = DOMPurify.sanitize(userComment);
> ```"

**解决方案2：使用安全渲染**
> "渲染用户生成的内容：
>
> ```typescript
> // ❌ 不好
> <div dangerouslySetInnerHTML={{ __html: userContent }} />
>
> // ✅ 好
> <div>{userContent}</div>
>
> // ✅ 如果需要HTML
> <div dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(userContent) }} />
> ```"

**预防：**
- 始终要求输入清理
- 使用安全的渲染方法
- 实施CSP

---

### 内存泄漏

**症状**：AI生成的代码导致内存泄漏（未清理的事件监听器、定时器、订阅）。

**根本原因：**
1. 清理未指定
2. 没有清理函数
3. 资源未释放

**解决方案：**

**解决方案1：要求清理**
> "实现实时数据更新功能。
>
> **清理要求：**
> - 在useEffect中返回清理函数
> - 移除事件监听器
> - 清除定时器
> - 取消订阅
> - 释放资源
>
> 示例：
> ```typescript
> useEffect(() => {
>   const interval = setInterval(() => {
>     // 更新逻辑
>   }, 1000);
>
>   return () => {
>     clearInterval(interval); // 清理
>   };
> }, []);
> ```"

**解决方案2：使用useEffect模式**
> "遵循此useEffect模式：
>
> ```typescript
> useEffect(() => {
>   // 设置
>   const subscription = dataSource.subscribe(data => {
>     setState(data);
>   });
>
>   // 清理
>   return () => {
>     subscription.unsubscribe();
>   };
> }, [dependency]);
> ```"

**预防：**
- 始终要求清理函数
- 使用useEffect清理模式
- 测试内存使用

---

### 竞争条件

**症状**：AI生成的代码在并发操作时失败（竞态条件、数据不一致）。

**根本原因：**
1. 并发未解决
2. 没有同步机制
3. 异步操作未正确处理

**解决方案：**

**解决方案1：要求同步**
> "实现购物车更新功能。
>
> **并发要求：**
> - 使用乐观更新
> - 实现请求去重
> - 使用锁或互斥锁（如果需要）
> - 处理冲突解决
> - 使用事务（如果适用）
>
> 示例：
> ```typescript
> const updateCart = async (itemId: string, quantity: number) => {
>   // 乐观更新
>   setCart(prev => updateItem(prev, itemId, quantity));
>
>   try {
>     // API调用
>     await api.updateCartItem(itemId, quantity);
>   } catch (error) {
>     // 回滚
>     setCart(prev => revertUpdate(prev, itemId));
>   }
> };
> ```"

**解决方案2：使用异步模式**
> "使用这些异步模式：
> - async/await（不是.then()）
> - Promise.all用于并行操作
> - Promise.race用于超时
> - AbortController用于取消
> - 错误边界用于错误处理"

**预防：**
- 始终要求并发处理
- 使用适当的异步模式
- 测试并发场景

---

### 紧密耦合

**症状**：AI生成的代码组件紧密耦合，难以测试或重用。

**根本原因：**
1. 架构未定义
2. 没有依赖注入
3. 直接依赖具体实现

**解决方案：**

**解决方案1：要求松散耦合**
> "实现用户管理功能。
>
> **架构要求：**
> - 使用依赖注入
> - 定义接口/类型
> - 使用策略模式
> - 避免直接依赖
> - 使组件可测试和可重用
>
> 示例：
> ```typescript
> // ❌ 不好 - 紧密耦合
> import { apiClient } from './api';
>
> // ✅ 好 - 松散耦合
> interface IApiClient {
>   getUsers(): Promise<User[]>;
> }
>
> class UserService {
>   constructor(private api: IApiClient) {}
> }
> ```"

**解决方案2：使用模式**
> "使用这些模式：
> - 依赖注入
> - 工厂模式
> - 策略模式
> - 观察者模式
> - 适配器模式"

**预防：**
- 始终要求松散耦合
- 定义接口和类型
- 使用设计模式

---

### 缺少文档

**症状**：AI生成的代码没有注释、JSDoc或README。

**根本原因：**
1. 文档未请求
2. AI专注于代码而非文档
3. 没有文档要求

**解决方案：**

**解决方案1：要求文档**
> "实现用户认证功能。
>
> **文档要求：**
> - 为所有函数添加JSDoc注释
> - 为复杂逻辑添加内联注释
> - 创建README.md解释用法
> - 提供使用示例
> - 记录API端点
>
> 示例JSDoc：
> ```typescript
> /**
>  * 使用电子邮件和密码验证用户
>  * @param email - 用户电子邮件
>  * @param password - 用户密码
>  * @returns 包含用户令牌的Promise
>  * @throws {AuthError} 如果凭据无效
>  */
> async function login(email: string, password: string): Promise<AuthResult> {
>   // 实现
> }
> ```"

**解决方案2：使用文档模板**
> "使用此文档模板：
>
> ```markdown
> # 功能名称
>
> ## 描述
> 简短描述
>
> ## 用法
> \`\`\`typescript
> // 示例代码
> \`\`\`
>
> ## API
> - `functionName(param)` - 描述
>
> ## 注意事项
> - 注意事项1
> - 注意事项2
> ```"

**预防：**
- 始终要求文档
- 使用JSDoc注释
- 创建README文件

---

## 工作流程瓶颈

### 重复相同的说明

**症状**：您发现自己每次都向AI解释相同的事情（项目结构、约定、模式）。

**根本原因：**
1. 没有持久指令
2. 没有项目上下文
3. 每次都从头开始

**解决方案：**

**解决方案1：创建.cursorrules**
> 在项目根目录中创建`.cursorrules`：
>
> ```markdown
> # 项目约定
>
> ## 技术栈
> - 框架：Astro + React
> - 样式：Tailwind CSS
> - 状态：Zustand
> - API：REST + JSON
>
> ## 代码风格
> - 2个空格缩进
> - 单引号
> - camelCase变量
> - PascalCase组件
>
> ## 模式
> - 使用`src/components/`中的组件
> - 使用`src/utils/`中的实用程序
> - 使用`src/api/`中的API调用
>
> ## 约束
> - 无外部依赖（除非已批准）
> - 最大文件大小：200行
> - 必须包含错误处理
> ```
>
> AI将自动遵循这些约定。

**解决方案2：创建项目知识库**
> 创建`docs/project-knowledge.md`：
>
> ```markdown
> # 项目知识
>
> ## 架构
> - 前端：Astro + React
> - 后端：Cloudflare Workers
> - 数据库：D1
> - 存储：R2
>
> ## 关键决策
> - 使用Zustand进行状态管理
> - 使用Tailwind进行样式
> - 使用D1进行数据存储
>
> ## 常见模式
> - API调用模式
> - 错误处理模式
> - 验证模式
> ```
>
> 在新会话中引用此文件。

**预防：**
- 早期创建.cursorrules
- 维护项目知识库
- 使用会话交接

---

### 难以恢复工作

**症状**：您无法轻松恢复之前的工作，忘记您在哪里或下一步是什么。

**根本原因：**
1. 没有文档
2. 没有状态跟踪
3. 没有任务分解

**解决方案：**

**解决方案1：创建current-state.md**
> 创建`docs/context/current-state.md`：
>
> ```markdown
> # 当前状态
>
> ## 已完成
> - [x] 项目设置
> - [x] 用户认证
> - [x] 用户资料
>
> ## 进行中
> - [ ] 用户搜索（50%完成）
>   - 实现了搜索UI
>   - 需要实现API端点
>
> ## 待办
> - [ ] 用户设置
> - [ ] 用户通知
> - [ ] 用户导出
>
> ## 下一步
> 实现用户搜索API端点
> ```
>
> 每次会话更新此文件。

**解决方案2：使用任务管理**
> 使用TodoWrite或Task Manager MCP：
>
> ```
> 创建任务：
> - 实现用户搜索API
> - 实现用户搜索UI
> - 测试用户搜索
>
> 更新任务状态：
> - 标记完成的任务
> - 添加新任务
> - 设置优先级
> ```

**预防：**
- 维护当前状态文档
- 使用任务管理工具
- 定期更新进度

---

### 调试时间太长

**症状**：调试一个问题需要很长时间，AI无法快速识别根本原因。

**根本原因：**
1. 诊断信息不足
2. 没有错误上下文
3. 没有复现步骤

**解决方案：**

**解决方案1：使用调试模板**
> "使用此调试模板：
>
> ```
> 错误：[粘贴确切错误]
>
> 堆栈跟踪：[粘贴堆栈跟踪]
>
> 相关代码：[粘贴文件:行]
>
> 复现步骤：
> 1. [步骤1]
> 2. [步骤2]
> 3. [步骤3]
>
> 预期：[应该发生什么]
> 实际：[发生了什么]
>
> 环境：
> - 浏览器：[版本]
> - Node.js：[版本]
> - 操作系统：[版本]
> ```
>
> 提供所有这些信息。"

**解决方案2：使用DevTools MCP**
> "使用DevTools MCP收集诊断信息：
>
> - 检查控制台错误
> - 检查网络请求
> - 检查性能
> - 检查存储
>
> 提供所有收集的信息。"

**预防：**
- 始终提供完整的错误信息
- 使用调试模板
- 使用DevTools MCP

---

### 破坏现有功能

**症状**：新代码破坏了现有功能，导致回归。

**根本原因：**
1. 没有集成测试
2. 没有审查更改
3. 没有向后兼容性要求

**解决方案：**

**解决方案1：要求向后兼容**
> "实现新功能。
>
> **兼容性要求：**
> - 不要破坏现有功能
> - 保持向后兼容
> - 不要更改现有API
> - 添加新功能而不删除旧功能
> - 测试所有现有功能
>
> 如果需要破坏性更改，请先询问。"

**解决方案2：要求测试**
> "在实现之前：
>
> 1. 为现有功能编写测试
> 2. 运行测试以确保它们通过
> 3. 实现新功能
> 4. 再次运行所有测试
> 5. 确保没有回归
>
> 如果任何测试失败，修复它们。"

**预防：**
- 始终要求向后兼容
- 编写集成测试
- 在合并之前审查更改

---

### 不清楚下一步

**症状**：您不知道接下来该做什么，任务没有明确分解。

**根本原因：**
1. 没有任务分解
2. 没有项目计划
3. 没有路线图

**解决方案：**

**解决方案1：使用TodoWrite**
> "使用TodoWrite创建任务列表：
>
> ```
> 项目：用户管理系统
>
> 阶段1：认证
> - [ ] 实现登录
> - [ ] 实现注册
> - [ ] 实现密码重置
>
> 阶段2：用户管理
> - [ ] 实现用户列表
> - [ ] 实现用户搜索
> - [ ] 实现用户编辑
>
> 阶段3：权限
> - [ ] 实现角色系统
> - [ ] 实现权限检查
> ```
>
> 按顺序完成这些任务。"

**解决方案2：使用OpenSpec**
> "使用OpenSpec生成项目计划：
>
> 1. 创建PRD（产品需求文档）
> 2. 运行OpenSpec生成规范
> 3. 审查生成的计划
> 4. 按阶段实施
>
> OpenSpec将自动分解任务。"

**预防：**
- 使用任务管理工具
- 创建项目计划
- 使用OpenSpec进行复杂项目

---

### 被复杂性压倒

**症状**：任务太大，您不知道从哪里开始。

**根本原因：**
1. 大任务未分解
2. 没有分阶段方法
3. 试图一次完成所有事情

**解决方案：**

**解决方案1：使用多步骤工作流程**
> "将此任务分解为步骤：
>
> **步骤1：设置**
> - 创建项目结构
> - 安装依赖
> - 配置工具
>
> **步骤2：核心功能**
> - 实现主要功能
> - 添加基本样式
> - 测试核心流程
>
> **步骤3：增强功能**
> - 添加额外功能
> - 优化性能
> - 改进UI
>
> **步骤4：测试和部署**
> - 编写测试
> - 修复错误
> - 部署到生产
>
> 一次完成一个步骤。"

**解决方案2：使用OpenSpec**
> "使用OpenSpec自动分解任务：
>
> 1. 提供项目描述
> 2. OpenSpec生成阶段和任务
> 3. 审查生成的计划
> 4. 按阶段实施
>
> 这将自动处理复杂性。"

**预防：**
- 将大任务分解为小任务
- 使用多步骤工作流程
- 使用OpenSpec进行复杂项目

---

### 代码风格不一致

**症状**：代码风格在整个项目中不一致（不同的命名、格式、模式）。

**根本原因：**
1. 没有记录约定
2. 没有样式指南
3. 没有代码审查

**解决方案：**

**解决方案1：创建样式指南**
> 创建`docs/style-guide.md`：
>
> ```markdown
> # 代码风格指南
>
> ## 格式化
> - 缩进：2个空格
> - 行长度：最大100个字符
> - 尾随逗号：必需
>
> ## 命名
> - 文件：kebab-case
> - 组件：PascalCase
> - 函数：camelCase
> - 常量：UPPER_SNAKE_CASE
>
> ## 模式
> - React：函数组件
> - Hooks：自定义hooks在`src/hooks/`
> - API：在`src/api/`
> - 工具：在`src/utils/`
> ```
>
> 所有代码必须遵循此指南。

**解决方案2：使用ESLint/Prettier**
> "配置ESLint和Prettier以强制执行代码风格：
>
> ```json
> {
>   \"extends\": [\"eslint:recommended\", \"plugin:react/recommended\"],
>   \"rules\": {
>     \"indent\": [\"error\", 2],
>     \"quotes\": [\"error\", \"single\"],
>     \"semi\": [\"error\", \"always\"]
>   }
> }
> ```
>
> 在提交之前运行linting。"

**预防：**
- 创建样式指南
- 配置linting工具
- 审查代码风格

---

### 忘记项目细节

**症状**：您忘记项目细节（技术栈、决策、约定）。

**根本原因：**
1. 没有知识库
2. 没有文档
3. 没有决策记录

**解决方案：**

**解决方案1：创建项目知识库**
> 创建`docs/project-knowledge.md`：
>
> ```markdown
> # 项目知识
>
> ## 技术栈
> - 前端：Astro + React + Tailwind
> - 后端：Cloudflare Workers
> - 数据库：D1
> - 存储：R2
>
> ## 关键决策
> - 使用Zustand进行状态管理（决策日期：2024-01-15）
> - 使用D1进行数据存储（原因：免费、快速）
> - 使用R2进行文件存储（原因：无出口费用）
>
> ## 约定
> - 2个空格缩进
> - 单引号
> - camelCase变量
>
> ## 模式
> - API调用模式
> - 错误处理模式
> - 验证模式
> ```
>
> 在新会话中引用此文件。

**解决方案2：记录决策**
> 创建`docs/decisions/`目录：
>
> - `001-tech-stack.md`
> - `002-state-management.md`
> - `003-database-choice.md`
>
> 每个决策文件包括：
> - 决策
> - 原因
> - 替代方案
> - 影响

**预防：**
- 维护项目知识库
- 记录所有决策
- 定期更新文档

---

### 高AI成本

**症状**：AI令牌成本太高，超出预算。

**根本原因：**
1. 提示效率低
2. 上下文重复
3. 使用昂贵的模型

**解决方案：**

**解决方案1：优化提示**
> "优化提示以减少令牌使用：
>
> - 使用模板引用而不是重复要求
> - 引用文档而不是粘贴内容
> - 简洁但具体
> - 删除不相关信息
>
> 示例：
> ```markdown
> ❌ 不好（冗长）
> 我需要你创建一个用户注册表单，包括以下功能：
> - 电子邮件字段，验证格式
> - 密码字段，最少8个字符
> - 确认密码字段
> - 提交按钮
> - 错误消息
> - 加载状态
> - 成功消息
> - ...
>
> ✅ 好（简洁）
> 创建用户注册表单（参考：docs/forms/registration-pattern.md）
> ```
> "

**解决方案2：使用更便宜的模型**
> "对于简单任务，使用更便宜的模型：
>
> - GLM-4-Flash：快速、便宜
> - Qwen-Coder：适合代码任务
> - Gemini Flash：快速响应
>
> 对于复杂任务：
> - Claude Sonnet：平衡
> - Claude Opus：仅在必要时
>
> 根据任务复杂性选择模型。"

**预防：**
- 优化提示
- 使用更便宜的模型
- 监控令牌使用

---

## 何时寻求帮助

### 何时自己解决

**您可以自己解决的问题：**
- 简单的语法错误
- 明显的逻辑错误
- 缺少导入
- 基本配置问题
- 文档化的问题

**使用这些资源：**
- [提示指南](../prompting/README-zh.md)
- [上下文管理](../context-management/README-zh.md)
- [故障排除指南](#快速症状查找)

### 何时询问AI

**AI可以帮助的问题：**
- 代码审查和优化
- 调试复杂问题
- 实现新功能
- 重构代码
- 编写测试
- 生成文档

**如何有效地询问AI：**
- 提供完整的上下文
- 使用具体的提示
- 引用相关文件
- 提供错误消息和堆栈跟踪
- 使用提示模板

### 何时询问人类

**需要人类帮助的情况：**
- AI无法解决的复杂架构问题
- 需要领域专业知识的问题
- 团队协调问题
- 客户沟通问题
- 商业决策

**在哪里寻求帮助：**
- 社区论坛（Stack Overflow、GitHub Issues）
- Discord社区
- 同行评审
- 导师或高级开发人员

---

## 调试检查清单

### 在询问AI之前

- [ ] 我是否收集了完整的错误信息？
- [ ] 我是否检查了控制台和网络标签？
- [ ] 我是否尝试了最小复现？
- [ ] 我是否搜索了类似问题？
- [ ] 我是否提供了完整的上下文？
- [ ] 我是否使用了调试模板？

### 在提交代码之前

- [ ] 代码是否编译无错误？
- [ ] 所有测试是否通过？
- [ ] 我是否审查了更改？
- [ ] 我是否检查了向后兼容性？
- [ ] 我是否更新了文档？
- [ ] 我是否运行了linter？

### 在发布之前

- [ ] Lighthouse分数是否> 90？
- [ ] 所有功能是否工作？
- [ ] 是否测试了移动响应？
- [ ] 是否测试了可访问性？
- [ ] 是否检查了安全性？
- [ ] 是否更新了版本号？

---

## 资源

### 相关文档

- [提示指南](../prompting/README-zh.md) - 有效的AI提示
- [上下文管理](../context-management/README-zh.md) - 管理AI上下文
- [质量标准](../quality-standards/README-zh.md) - 专业质量
- [工作流程](../workflow/README-zh.md) - 开发工作流程

### 工具

- [Claude Code CLI](../development-tools/recommended-tools/claude-code-cli-zh.md) - CLI工具
- [Cursor](../development-tools/ide/cursor-zh.md) - IDE
- [DevTools MCP](../development-tools/mcp-servers/devtools-mcp-zh.md) - DevTools集成
- [TodoWrite](../development-tools/mcp-servers/todowrite-mcp-zh.md) - 任务管理

### 社区

- [Discord](https://discord.gg/vibecoding) - 社区支持
- [GitHub Issues](https://github.com/vibecoding/guide/issues) - 报告问题
- [Stack Overflow](https://stackoverflow.com/questions/tagged/vibecoding) - 问答

---

**返回**：[主指南](../../README-zh.md)